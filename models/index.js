const User = require('./User');
// const Post = require('./Post');
// const Notes = require('./placeholder');

// Notes.belongsTo(Post, {
//     foreignKey: "post_id"
// });

// Notes.belongsTo(User, {
//     foreignKey: "user_id"
// });

// Post.belongsTo(User, {
//     foreignKey: "user_id"
// });

// Post.hasMany(Notes, {
//     foreignKey: "post_id"
// });

// User.hasMany(Post, {
//     foreignKey: "user_id"
// });

// User.hasMany(Notes, {
//     foreignKey: "notes_id"
// });

module.exports = {
    User
    // Post,
    // Notes,
};